({
    sendComment : function(component, event, helper) {
        helper.doSendComment(component, event);
    },

    onChange : function(component, event, helper) {
        helper.doChange(component, event);
    },
})